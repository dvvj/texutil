package org.ditw.matcher

object CompMatcherNXs {
  import CompMatchers._

//  private[matcher] class CmLookAround (
//    private val sfx:Set[String],
//    private val sfxCount:Int,
//    private val mLeft:TCompMatcher,
//    private val mRight:TCompMatcher,
//    val tag:Option[String]
//  ) extends TCompMatcher with TCompMatcherN {
//
//    protected val subMatchers:Iterable[TCompMatcher] = Iterable(mLeft, mRight)
//
//    override def runAtLine(
//      matchPool: MatchPool,
//      lineIdx: Int): Set[TkMatch] =
//      super.runAtLine(matchPool, lineIdx)
//  }
}
